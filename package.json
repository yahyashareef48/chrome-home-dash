{
  "name": "chrome-home-dash",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc && xcopy /E /I /Y public dist",
    "watch": "tsc --watch",
    "build:zip": "npm run build && powershell Compress-Archive -Path dist/* -DestinationPath extension.zip -Force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/dompurify": "^3.0.5",
    "@types/marked": "^5.0.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "dompurify": "^3.3.0",
    "marked": "^16.4.1"
  }
}
